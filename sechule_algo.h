#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>

typedef struct Game Game;
typedef struct Day Day;
typedef struct  Player Player;
typedef struct Team Team;
typedef struct League League;

int numCommonGames (Team, Team);